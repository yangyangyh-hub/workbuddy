# PROJECT —— 个人工作台(daka)

> 每次会话开工必读。有变化就**更新本文件**,不新写第二份。
> 全局协作规则放在本机的 WorkBuddy 配置目录(`~/.workbuddy/RULES.md`),**不进本仓库**。
> **本文件只管"项目状态"**:要什么看 `PRD.md`,怎么建看 `AGENTS.md`。三份分工,不互相复制。

## 0. 三份文档的分工

| 文件 | 回答什么 | 改的时机 |
|---|---|---|
| `PROJECT.md`(本文件) | 现在做到哪、卡在哪、要你拍什么板 | 每次会话收尾 |
| `PRD.md` | 要做什么(产品定义、口径、空状态/失败态) | 需求变更 |
| `AGENTS.md` | 怎么建(目录结构、加载顺序、禁止事项) | 工程约定变更 |

## 1. 任务(Tasks)

- [x] T1 需求定型:单文件 HTML、零依赖、localStorage(09-08)
- [x] T2 第一版静态外壳 Mock(index.html + css/style.css)(09-09)
- [x] T3 写 `PRD.md`(数据 key 前缀 `workbench.*`、口径、空/失败态)(09-11)
- [x] T4 写 `AGENTS.md`(目录结构、加载顺序、金额以「分」存整数)(09-13)
- [x] T5 今日看板加「存储小实验」临时块(09-14) → **09-15 已删除**(拆骨架前先拿掉,那块直接调 localStorage)
- [x] T6 拆 `js/` 多文件骨架(09-15 完成:storage.js 统一读写 + 7 个业务模块占位 + app.js 启动/导航/看板刷新)
  - ⚠️ 已处理:存储小实验连同其直连 localStorage 的代码一并删除,与 AGENTS「只经 storage.js」不再冲突
- [x] T7 待办模块(09-16 完成:三 Tab + 添加/勾选/删除/右侧编辑 + 截止日期文案 + 关联笔记 + 看板联动)
- [x] T8 快速笔记模块(09-17 完成:提交制 + 列表/详情 + 删除 + 与待办互跳并高亮)
- [x] T9 打卡模块(09-20 完成:四类固定 + 每日/每周目标 + 分周期进度 + 历史保留)
- [x] T10 记账模块 —— **手动记账部分(09-20 完成)**:类型/金额/分类/备注 + 今日本周本月 + 明细删除
- [x] T10b 记账 OCR(09-21 完成:粘贴/上传图片 → 识别 → 确认卡 → 入账 source=ocr;版本锁 7.0.0)
- [x] T11 快捷入口模块(09-18 完成:常用网站 + 常用文本 + 首次预置 + 复制兜底)
- [x] T12 数据管理:导出/导入 JSON(schemaVersion=1)(09-22 完成:页脚三按钮 + 摘要确认 + 二次确认清空 + 回滚)
- [x] T14 番茄钟(2026-09-23 完成:看板 hero 内嵌极简版,25/5 写死 + 圆环 + 开始/暂停/重置 + 双段切换;纯内存不落库)
- [x] T15 待办优先级(2026-09-23 完成:high/mid/low + 列表小标点击循环 + 详情下拉 + 排序;旧数据按「中」兼容,不动 schemaVersion)
- [x] T16 全站换肤 + 深色模式(2026-09-24 完成:奶油暖色系三色卡片 + 内联 SVG 导航图标 + 深浅色切换;验收脚本 52 项全通过)
- [x] T17 打卡开放增删改(2026-09-24 完成:打卡项可新增/改名/改类型/改周期/改目标/删除 + 空状态;老数据平滑升级;**80 项验收全通过**)
  - 同时:品牌字号 24 → 21px、「打卡」导航前移到第 2 位
- [x] T18 打卡「−1」纠错(2026-09-24 完成:多次型减到 0 为止、不留 0 值记录;一次型减 1 = 撤销完成;当前为 0 时按钮禁用;**52 项验收全通过**)
- [x] T19 站名移到页面中间 + 左栏改昵称(2026-09-24 完成:站名用书法体放在日期与番茄钟之间真居中;左栏改为「Hi，昵称」可自己填;**54 项验收全通过**)
- [x] ~~T20 待办子任务~~(**2026-09-24 当天撤销**:曾实现每条待办可挂 ≤20 个子任务、整条完成状态由子任务推导;yang 看到实现后明确「不需要设置子待办」,改为下面的进度档位。相关数据层、渲染、事件、样式、验收用例**已整段移除**)
- [x] T21 待办进度档位(2026-09-24 完成:每条待办一个 `progress` 值 0/20/40/60/80/100,列表改卡片 + 进度条 + 五个档位按钮;再点当前档位退回 0%;`done` 由 `progress` 派生;旧数据零迁移;**100 项验收全通过**)
- [x] T22 多用户版方案定论(2026-09-24:确认**不做**账号 / 登录 / 后端;"一个页面、各存各的浏览器"已满足需求;四条数据边界写进 PRD 新章节)
- [x] T23 PRD 全面对齐实现(2026-09-24:**8 处**修正 + 新增「多人与共用链接的数据边界」;其中"记账/快捷入口可编辑""无数据显示 `—`"两处按**实现**改,而不是反过来补功能)
- [x] T24 装霞鹜文楷(2026-09-24:v1.522 Regular → **用户级**字体目录 `%LOCALAPPDATA%\Microsoft\Windows\Fonts`;**验 TTF name 表确认家族名 `LXGW WenKai` 与 CSS 匹配 → 零代码改动**)
- [x] T13a **部署静态站点(2026-09-24 完成)**:`https://personal-workbench-89221.app.workbuddy.host/`
  - 部署源目录 = 项目根 `D:\workbuddy\daka`(即"改了根目录 = 下次发布的内容"),应用绑定在这个目录上,以后重新发布链接不变
  - 管理入口:应用内「设置 — 数据管理 — 应用」(不需要另开控制台认领)
  - ⚠️ **origin 变了,老数据不会跟过去**:`127.0.0.1:8777` 里的 localStorage 与线上是两套。要迁移先在旧的导出 JSON,再在新链接里导入
  - ⚠️ **整个目录都会被公开可读**(已实测):`/.workbuddy/memory/*.md`、`PROJECT.md`、`PRD.md`、`AGENTS.md` 都能直接访问,且目录列表是开着的(`python http.server` 默认行为)。里面没有密钥、证件、图片,也**没有任何 localStorage 数据**(它只存在浏览器里,不可能被上传)
- [ ] T13b Day 18-19 部署前检查:清掉 §7 里待确认的 PRD 口径问题;打包 zip 更新资料库里那份 09-14 快照

## 2. 需求(Requirements)

- **功能性**:左侧导航 6 个模块(今日看板、待办、快速笔记、记账、快捷入口、打卡)+ 看板内嵌番茄钟 + 深色模式开关;数据只存本机
- **非功能性**:
  - 零依赖、零构建、无登录无后端
  - 视觉(**2026-09-24 改版**):奶油纸底 + 细点阵;卡片圆角 16px、近乎无边框,薄荷 / 粉 / 杏三色平涂;强调色深墨绿;数字等宽 `tabular-nums`
  - 深浅两套主题,偏好存 `workbench.meta.theme`
  - 只做电脑网页,不做移动端断点

## 3. 目标(Goals)

- 交付物:`index.html`(双击即用)+ 部署版静态站
- 成功标准:21 天内收尾;前 17 天本机真实使用,Day 20 上线后数据靠导出/导入迁移

## 4. 约束(Constraints)

- 技术栈:原生 HTML + CSS + JS;localStorage;OCR 用 Tesseract.js(本地 `vendor/`,懒加载)
- **明确不做的事**:六宫格布局、任何图表库、提醒通知、富文本/Markdown、手机端、营销口号/标语
- **视觉禁令(2026-09-24 重新表述)**:原写的是「禁止紫粉渐变和玻璃拟态」。改版后**颜色不再限冷灰蓝**,
  禁的东西收窄为 **渐变(任何色相)、玻璃拟态(backdrop-blur / 半透明叠层)、重阴影**。
  现在的粉是平涂色块(`#fbdfe2`),不是渐变也不是紫,不冲突。
- **主题(2026-09-24 新增)**:浅色 / 深色两套,按钮在左侧栏底部。**只做颜色,不做布局差异**
- **番茄钟(2026-09-23 放宽)**:原禁令里的"番茄钟/实时计时器"解除,但**只做"极简节拍器"**——
  内嵌在今日看板(**不加左侧导航项**)、25/5 写死、不跨刷新续跑、不通知/不出声、
  不统计番茄数、不与打卡联动、不做自定义时长。超出这些的就是超范围。
- **安全底线**:用户输入一律 `textContent` 不拼 `innerHTML`;OCR 结果**绝不经人工确认就入账**;localStorage 禁存密码/Key/证件照片

## 5. 当前状态(Status)

- 已完成:三份文档 + js/ 骨架 + 今日看板 + 六个业务模块(待办/笔记/快捷入口/打卡/记账/OCR) + 数据管理(T12) + 番茄钟(T14) + 待办优先级(T15) + **全站换肤与深色模式(T16)** + **打卡开放增删改(T17)**
- **09-24 已部署上线**:`https://personal-workbench-89221.app.workbuddy.host/`(静态站,HTTPS)
  - **09-24 14:20 第二次发布**:把当天下午积累的 4 批改动一次性同步上线
    (缓存串修复 + 删提示文字 + 字号字体 + 打卡开放增删改),
    并逐项 curl 验证:导航顺序 `board/checkin/todo/note/ledger/links`、
    `--brand-size: 21px`、`habitAddForm` 在位、旧提示文字计数 0、旧色值 `#dae1e8` 计数 0、
    `habits.js` 含 `initialized` 标记与新 API。
  - **这次发布后,以后改 css/js 不用再让用户强刷** —— `?v=` 版本号会换掉缓存键。
    唯一的例外:改 `index.html` 本身仍需一次强刷(入口文件没法给自己加版本号)。
  - **09-24 14:28 第三次发布**:打卡「−1」上线,逐项 curl 验证 ——
    缓存串统一为 `?v=20260924e`、`habits.js` 含 `function unbump` 且已对外导出、
    `app.js` 含 `habit-down` 与 `action === 'down'` 分支、`style.css` 含 2 条 `.habit-down` 规则、四个资源全 200。
- **09-24 15:36 第四次发布**:T19(站名/昵称)+ T21(待办进度档位,含 T20 撤销)一起上线,逐项 curl 验证:
  缓存串统一 `?v=20260924j`;首页含 `#todoTicks`,子任务元素与 `#todoDone` **全部归零**;
  `style.css` 含 `.todo-card`(13 处)与 `.tick.is-current`(2 处),旧 `.todo-item` / `.todo-sub` 已清零;
  `todo.js` 含 `applyTick` / `getPercent` / `isDone`;`app.js` 含 `fillTickRow` / `buildTodoCard`,
  而 `writeSubtasks` 与 `buildTodoRow` 已清零;四个资源全 200。
  - ★ 线上 `todo.js` 裸 grep `subtask` 还有 2 处 —— **剥掉注释再查,确认是头部历史注释,代码零残留**。
    裸 grep 就下结论是错的做法,这条已写进新技能 `jsdom-acceptance-testing` 的坑 3
- 进行中:等 yang 在线上点一遍待办的进度档位(手感:卡片在半宽栏下的观感、700ms 延迟移动是否舒服);
  以及**重启浏览器**后看大日期是否真的变成霞鹜文楷
- 待办:T13b 剩下的**资料库快照更新**(PRD 口径对齐已于 09-24 完成);§7 第 21 条(要不要换发布源目录)待定
- 阻塞项:无

**09-24(晚)站名移到页面中间 + 左栏改昵称(T19)**

- 站名(`h1.hero-title`)从左侧栏移到看板 hero,位置在**日期块与番茄钟之间**。
- **居中用 `1fr auto 1fr` 三栏网格,不用 flex 居中**:左右两块宽度差很大(日期块 vs 番茄钟),
  flex 的 `justify-content:center` 会被挤偏;三栏网格才能保证中间那栏落在页面正中。
- 字体新增 `--font-title`(方正舒体 → 华文新魏 → 华文行楷 → 文楷 → 楷体),
  配 `--title-size: 30px`。**书法体只有一个字重,`font-weight` 必须写 400**,否则浏览器合成加粗会把笔画糊掉。
- 颜色用 `--accent`(深墨绿),不黑;底下用 `::after` 伪元素垫一道 `--tint-mint` 薄荷记号笔,
  做出"划重点"的手感(不用图片、不用渐变)。
- 左栏 `.brand` 里的站名换成「Hi，昵称」,昵称是**可直接编辑的输入框**(与打卡名称同一套做法:
  平时像纯文本,悬停显虚线边,聚焦变实线),存 `workbench.meta.nickname`,限 8 字。
- 昵称与主题一样算"显示偏好",`confirmClear()` 里在 `clearAll()` **之前**读出并写回,
  否则清空数据会把昵称一起清掉(这个坑主题那次已经踩过一次,这次提前避开了)。
- 顺手清掉已无人使用的 `--brand-size`(原给左栏站名用),改为 `--greet-size: 16px` 给问候语。
- 缓存串升到 `?v=20260924f`;验收脚本 `check-title.js`:**54 项全通过**
  (含"hero 子元素顺序必须是 日期→站名→番茄钟"、昵称截断/去空格/清空后保留/刷新恢复、其它模块回归)。
- **14:53 微调**(yang 反馈):① 字号 30 → **38px** ② 位置改为**紧贴日期**、不再居中
  ③ **书法体他不喜欢**,换成**幼圆**。
  - 位置改法:`.hero` 从 `1fr auto 1fr` 改成 **`auto auto 1fr`** ——
    前两栏按内容宽、第三栏吃掉剩余空间,站名自然贴在日期旁边,番茄钟仍被推到最右。
    `column-gap: 14px` 控制站名与日期的距离。记号笔加厚到 9px、下移 1px 配大字号。
  - 新增 `--title-weight`:**单字重的字体(幼圆/书法体)必须配 400**,
    多字重的黑体(雅黑)才写 700 —— 换个字体要同时看这两行。
  - 已给 5 个候选字样(幼圆 / 方正姚体 / 华文中宋 / 雅黑特粗 / 双色混排),
    **yang 选定 D:微软雅黑特粗 + 大字距**。为什么 D 最合适:雅黑/PingFang 几乎每台机器都有,
    跨设备不走样;活泼感靠字重与字距拿,不依赖稀有字体。
    三个变量一次配好:`--font-title`(雅黑 → PingFang → Noto)、`--title-weight: 700`、`--title-spacing: 5px`。
  - 本轮只改 CSS,`check-title2.js`:**29 项全通过**(布局列、字号、字重变量、其它模块回归)。
    改完字体后 `check-title3.js`:**28 项全通过**。缓存串 `?v=20260924h`。

**09-23 番茄钟(T14)落地说明**

- 位置:`index.html` 的 `<header class="hero">` 内右侧。`.hero` 改成 flex 两端对齐,
  `.hero-main` 在左、`.pomo` 在右。**没有动下方「本日待办 / 打卡一览」两块同宽同高的布局。**
- 代码都在 `js/app.js` 的「番茄钟」小节里(约 120 行)。没新建 `js/pomo.js` ——
  它不碰数据、不碰 storage,按 AGENTS「app.js 管启动与看板绘制」的分工放在这里更合适,
  也避免动 AGENTS §目录那张已经稳定的树。
- **计时口径**:存「目标结束时刻 `endsAt`」,每次 tick 用 `endsAt - Date.now()` 反推剩余,
  不是每秒减一。原因:标签页切到后台后 `setInterval` 会被节流到几秒一次甚至更慢,
  累减会明显走慢;按结束时刻算,回到前台一步就校准回来。验收里用篡改 `Date.now` 验证过。
- 圆环:SVG `stroke-dasharray = 周长`,改 `stroke-dashoffset` 控制露出多少,随时间递减。
  `r=39` 与 `app.js` 里的 `POMO_RING_LENGTH` 是同一对数,改一个必须改另一个。
- 到点只停表 + 改文案 + 圆环走完,不弹窗、不出声、不自动切到下一段(超出极简范围的事都没做)。
- `W.app.pomo` 暴露了 start/pause/reset/setPhase/state,是给验收脚本驱动的入口,界面用不到。

**09-23 待办优先级(T15)落地说明**

- 新增 `priority` 字段,取值 `high|mid|low`,新增时默认 `mid`。
- **旧记录没有这个字段也能正常显示与排序**(缺字段按「中」),所以
  **schemaVersion 保持 1、不写迁移逻辑**;真到改结构那天再整批升版本。
- 排序口径:`未完成在前 → 高/中/低 → 创建时间先后`,写在 `js/todo.js` 的 `sortItems()`。
- 两处 UI:列表左侧可点的小标(点一下 高 → 中 → 低 循环) + 详情区下拉(改完点保存)。
  看板「本日待办」显示同一枚小标,只读不可点。
- 详情「保存」之前漏了读取 `#todoPriority`,一并补上 —— 否则下拉改了不生效。

**09-24 全站换肤 + 深色模式(T16)落地说明**

- 起因:yang 给了一张参考图(打工人小账本),要求「换个 UI 风格,参照这样子的」。
  该图是**暖色系**,与既定的「冷灰蓝」正好相反 —— 已当面向 yang 指出这个冲突,他选择**照搬暖色**,
  于是 AGENTS §设计要求 与 PROJECT §2/§4 里的冷灰蓝约束**同步作废并改写**
  (不改文档的话,下次会话看到暖色界面会以为是 bug 又改回去)。
- 改法是**整份 `css/style.css` 重写**(原 1478 行 / 21 个组件区块),逐段保留全部选择器,只换视觉层。
  没有逐条改的原因:颜色散落在几十处硬编码里,局部改必留死角。
- **颜色全部收进变量**:`:root` 是浅色,`html[data-theme="dark"]` 是深色覆盖块。
  选择器故意写成 `html[...]` 而不是 `[data-theme=...]` —— 后者与 `:root` 同为 (0,1,0) 权重,
  命中顺序一变就失效。
- 新增三组变量:`--tint-mint/pink/amber`(三色卡片)、`--chip-bg/--chip-line`(小色块底)、
  `--prio-*`(优先级三色,原来写的是 rgba 硬编码,深色下会错)。
- 深色模式:左侧栏底部新增切换按钮,偏好写进 `workbench.meta.theme`,
  **不新增 localStorage key**(验收里断言了 key 总数仍是 7 个)。
- 图标:**内联 SVG**,统一 `.icon` 类(`stroke: currentColor`)。6 个导航图标 + 品牌标 + 3 张卡片角标 +
  太阳/月亮两个。没有图标字体、没有新增图片文件;点阵底纹是 CSS `radial-gradient` 画的。
- 结构风险点已验:导航绑定用的是 `closest('.nav-item')` + `dataset.page`,**不读 `textContent`**,
  所以往按钮里塞 `<svg>` 不会破坏切页 —— 连「点在图标上」这条路径也断言过。
- **抓到并修掉一个真 bug**:清空数据时 `readTheme()` 在 `clearAll()` **之后**才调用,
  那时 `meta` 已被删掉,结果主题被写成 `light`(界面还是深色、数据却错了,要等刷新才暴露)。
  修法:在 `clearAll()` 之前先把主题读进局部变量。
- 验收脚本 `check-theme.js`:**52 项全通过**。除功能回归外特意加了 CSS 静态自检:
  ① 组件样式里不许再有十六进制 / rgb 硬编码;② 深色块必须覆盖全部颜色变量(漏一个就在深色下露浅底);
  ③ 用亮度公式算浅色 `--ink` 够深、深色 `--ink` 够亮(防「深底深字」);④ 旧冷灰蓝的 10 个色值已清干净。
- **09-24 已同步到线上**(yang 授权后重新发布,链接不变),线上内容逐项 curl 验过:
  `style.css` 33KB、含 `html[data-theme="dark"]` 与 `--tint-mint`、旧色值 `#dae1e8` 计数为 0;
  首页含 `themeToggle`、9 处 `.icon`、`card-mint`。
- 缓存提醒:静态站没有做文件名 hash 或版本查询串,**改版后自己和他人都可能要 Ctrl+F5** 才看到新版。

**09-24 线上事故:新 HTML 配旧 CSS(已定位并修复)**

- **现象**(yang 截图):线上页面能打开、布局大框架还在,但**所有图标变成大黑块**——
  左侧品牌标、6 个导航图标、3 张卡片角标全成了纯黑圆角方块,而且把文字挤到了下一行;
  卡片还是白的(没有三色)、背景还是旧的灰蓝。
- **根因**:浏览器用了**缓存的旧 `style.css`**,而 `index.html` 是新版。
  旧 CSS 里没有 `.icon / .card-mint / .card-icon / .brand-mark` 这些新类,
  于是 `<svg class="icon">` 退化成 SVG 默认表现:**`fill` 默认黑色 + 无尺寸 → 300×150 大黑块**。
  所有症状(黑块、导航文字换行、白卡片、旧配色)全部由这一条解释。
- **不是部署失败**:线上文件已确认是新版 —— `style.css` 34256 字节、`index.html` 29255 字节,
  与本地字节数一致,`Last-Modified` 也一致;`#dae1e8` 计数 0、`--tint-mint` 计数 4。
- **为什么会被缓存**:服务端(CloudStudio Gateway → `python http.server`)响应里**只有 `Last-Modified`,
  没有 `Cache-Control`、没有 `ETag`**,浏览器可以按启发式规则直接复用缓存而不回源校验。
- **两处修复**:
  1. **缓存串**:`index.html` 里 1 个 css + 8 个 js 引用全部加上 `?v=20260924`。
     换 URL 就等于换缓存键,以后改完发布不需要用户强刷。规则已写进 `AGENTS.md`
     ——「改 css/js 必须同步改版本号」,否则会再犯。
  2. **图标兜底**:12 个内联 SVG 全部补上 `width`/`height`/`fill="none"`/`stroke="currentColor"` 属性。
     这样即使 CSS 过期或没加载,图标也只是"没风格",不会变成黑块把布局撑烂。
- 验收脚本 `check-cache.js`:**25 项全通过**(图标属性齐备、9 条引用版本号一致且文件都存在、
  并在"剥掉 CSS"的环境下单独跑了一遍确认图标仍是小尺寸)。

**09-24(下午)删掉三处提示文字 + 字体**

- yang 圈出三处要删:左栏底部「本机 · 未同步」、页脚两行「个人工作台 · 数据保存在本机浏览器 /
  今天我们在看前端…」。三处全部删除,`index.html` 与 `style.css` 里对应规则一并清掉
  (`.rail-foot`、`.foot-line`),`.foot-actions` 的虚线上分隔也随之去掉。
- **有意保留**:OCR 那段「图片只在本机浏览器里处理,不上传、不保存原图」—— 那是隐私说明,不是冗余提示。
  验收脚本里特意加了一条断言防止以后被误删。
- 左栏结构变成:品牌标 → 6 项导航 → 主题开关(`.rail-tools` 的 `margin-top:auto` 把它推到底部)。
- **字体(09-24 已定:方案 D + 字号放大一号)**。yang 要左栏更鲜活,给了 4 个方案样张,他选 **D**:
  **不引 webfont**,靠"常见黑体 + 加大字重 + 拉开字距"拿精神。
  - 新增三个变量:`--font-brand`(`"Microsoft YaHei", "PingFang SC", "Noto Sans SC", system-ui, sans-serif`)、
    `--brand-size`(19 → 24 → **最终 21px**)、`--nav-size: 15px`(原 14)。
    **两个字号的变量单独提出来 —— 以后想再调大小只改这两行。**
    (yang 先嫌小要放大,放到 24px 又觉得偏大,回调到 21px。提成变量的价值在这里体现了。)
  - 品牌名:雅黑 700 / 字距 3px / 用 `--ink`;导航项:500(选中态 600,靠字重差区分)。
    雅黑排第一是为了拿到**真粗体**(本机装有 `Microsoft YaHei Bold`),后面 PingFang(mac)、Noto(Linux) 同类回退不会走味。
  - 顺带把品牌标从 38px 提到 42px、标内图标 20 → 22px,免得文字变大了图标显小。
  - ⚠️ **坑点记录**:`.nav-item` 里有 `font: inherit` 简写,**`font-size`/`font-weight` 必须写在它之后**,
    否则会被简写重置回继承值。验收脚本专门断言了这三条属性的先后顺序。
  - 验收脚本 `check-font.js`:**40 项全通过**(含字号确实变大、字体栈不含稀有字体、简写顺序、深浅两套未被破坏、功能回归)。
  - ⚠️ **重要发现**:`--font-serif` 第一顺位写的 **霞鹜文楷本机并没有装** ——
    所以「个人工作台」现在实际是回退到**楷体**在渲染,不是文楷。之前一直没验证过这件事。
  - 本机确实可用的候选:幼圆、华文新魏、方正舒体/姚体、华文楷体/中宋/仿宋/彩云、
    微软雅黑(含 Bold/Light)、等线、Noto Sans SC / Noto Serif SC、黑体、宋体。
- 跨设备风险:幼圆/华文/方正这类字体**别人的电脑不一定有**,回退后风格会变。
  要绝对一致得引 webfont,而中文字体动辄数 MB(且方正/华文不可再分发),不值当 —— 已把这点讲清。
- 本轮改动后缓存串升到 **`?v=20260924b`**;验收脚本 `check-cleanup.js`:**30 项全通过**
  (三处文字确已消失、数据管理 11 个元素未误删、导航/主题/番茄钟/待办/清空面板全部回归)。

**09-24(晚)待办进度档位(T21),同时撤销 T20 子任务**

**第一轮我理解错了。** yang 说「待办增加一个进度条,不要只是完成和未完成」,我把它读成
"给待办加子任务",出了三个方案样张(① 列表顶部总进度 ② 每条带子任务清单 ③ 每条手动填百分比),
他选了 ②,于是做完 T20(88 项验收通过)。**结果这不是他要的** —— 他随后贴了参考图:
每条待办一条进度条 + `20% / 40% / 60% / 80% / 完成` 五个按钮,并明确
「不需要设置子待办,也不需要给所有待办设置一个任务条」(后半句 = 否决方案 ①)。
**T20 当天整段撤销。**

- 我原先判断「手动维护百分比存活率很低」,那是针对**拖滑块**说的;他要的是**点档位** ——
  一下一个动作,不用判断"我到底做了百分之几"。**这个判断我当场收回并写明**。
- **数据结构**:待办只加**一个字段** `progress`,取值 **0/20/40/60/80/100**,100 即完成。
  **`done` 变成派生值**:写入时同步维护,读取统一走 `isDone(item)`(只看 progress)。
  这样即使字段被手改脏了,也不会出现「进度 100% 却显示未完成」这种矛盾。
- **旧数据零迁移**:没有 `progress` 的按 `done` 反推(true→100 / false→0),schemaVersion 不动。
- **反转退回**:界面上没有「0%」按钮,靠**再点一次当前档位**回到 0%;「完成」按钮走同一条逻辑,
  所以完成后再点它就是取消完成。这条落在数据层 `applyTick()`,UI 只负责传 tick 值。
- **脏值**:`progress` 不是数字 → 回落 `done`;是数字但不在档位上 → 归到**最近的档位**
  (99→100、85→80,阈值 90;候选里含 100,不做特判)。
- **列表改卡片**:① 优先级 + 标题 + 截止日期 + 删除(改成图标) ② 进度条 + 百分比 ③ 档位按钮。
  列表栏从 `1fr 1fr` 调成 `1.35fr 1fr` —— 5 个档位按钮并排需要横向空间。
- **详情区与卡片共用 `fillTickRow()`**,两处实时同步;看板「本日待办」每条跟一条迷你进度条 + 百分比。
- **过渡保留**(yang 之前明确要过):点「完成」翻转后**不立刻重排**,先原地更新该卡片,
  700ms 后再重排 + 淡入。`buildTodoCard()` 供整体重排和原地更新共用。
- 缓存串升到 **`?v=20260924j`**;验收脚本 `check-progress.js`:**95 项全通过**。

**撤销子任务时保留的两条纪律**

1. **不主动删用户的存储**:遗留的 `subtasks` 字段**原样留在 localStorage 里**,只在读取/渲染时忽略。
   删数据是不可逆动作,哪怕这个功能已经废掉也不做 —— 验收里专门断言了这一点
   (断言"字段还在"而不是"字段没了")。
2. **`buildTodoCard()` 供整体重排与原地更新共用**:两处各写一份构建逻辑,迟早改一处漏一处。

**T20 阶段抓到并修掉的两个真 bug**(功能虽已撤,这两条规律仍然成立)

1. **`getSubtasks` 直接返回原始数组,没清洗** → 脏数据里的 `null` 条目在渲染时抛
   `Cannot read properties of null`,**整页崩掉**。修法:读取路径统一过 `sanitizeSubtasks()`,
   它同时负责丢 `null` / 丢空文本 / 截断超长 / 截到上限。
   **id 兜底故意用下标而不是随机值** —— 随机 id 每次渲染都变,点上去就找不到那一项。
2. **延迟重排的挂起标志设晚了**。原来写成"先执行写入、看返回值再决定挂不挂",但数据层写完会
   **同步**派发事件触发刷新,等写入函数返回时行早就跳走了 → 过渡完全不生效(验收里 100% 复现)。
   修法:包一层 `writeSubtasks()`,**写之前**就把列表挂住,写完再按是否翻转决定"延时重排"还是"立即正常重排"。
   两条教训都写进了 `AGENTS.md` 开发规范。

**09-24(傍晚)打卡开放增删改(T17)+ 字号回调 + 导航调序**

yang 要求:①「个人工作台」字号回调 ②「打卡」挪到导航第 2 位 ③打卡模块开放 —— 内容可编辑、类型可改、
可删除、可新增。第三项是**范围变更**:原 PRD §5 明写「四类固定,不让增删种类」。

**数据层(`js/habits.js` 重写)**
- `label` / `kind` / `period` / `target` / `order` 全部搬进 `config[id]`,打卡项列表由 config 动态生成,
  不再有任何硬编码的 `MODULES` 数组。
- **老数据的四个 key(`water`/`sport`/`study`/`work`)原样保留**:记录是按 id 存的,**换 id 等于把历史记录全丢掉**。
  老 config 里没有 `label`/`kind` → 读取时按内置的 `LEGACY` 表补上(喝水/学习=多次型,运动/工作=一次型),
  目标与周期原样保留。所以老用户升级后一切照旧,零迁移动作。
- **新增 `initialized` 标记 —— 这是本轮最容易做错的地方。** 如果只按「config 为空就种回四项」判断,
  用户把打卡项全删光后,下次读取又会自动种回来,用户会以为删不掉。现在写成
  「**从没初始化过** 且 config 为空」才种,任何一次写入都会打上 `initialized: true`。
  这和常用网站的 `meta.linksSeeded` 是同一类坑,已把规律写进 `AGENTS.md` 开发规范。
- `records` 里暂时对不上 config 的 key **不再丢弃**(原实现按 MODULES 过滤)。原因:读取时顺手清理,
  一旦哪次读到配置异常就会把历史记录悄悄抹掉。删除打卡项时是**显式**清理它的记录(含清空的桶一并去掉)。

**界面(`js/app.js` + `index.html` + `style.css`)**
- 卡片标题改成**可编辑输入框**(平时看不出是输入框,悬停才出现虚线边,聚焦变实线)——这是"内容可编辑"的最直接做法。
- 卡片右上角加「删除」;设置区加「类型」下拉(原来是只读徽章)。
- 打卡页顶部加新增表单:名称 + 类型 + 周期 + 目标次数 + 添加。选「一次型」时目标输入框自动禁用并锁为 1。
- **补上两个空状态**(打卡页 + 看板「打卡一览」),顺带解决待澄清第 11 条;看板汇总卡在总数 0 时显示「还没有打卡项」。
- 名称改成空会被拒绝并回退原值;超长名字截断到 12 字;一次型目标强制 1;多次型目标不允许小于 1。
- 删除前二次确认,文案写明「会连同 N 个周期的记录一起删除」(`countRecords()` 算出来给用户看)。
- 字号:24 → 21px。导航:打卡挪到第 2 位(验证过切页与高亮都正常,导航绑定走 `dataset.page` 不看顺序)。
- 顺手改掉`.ck-name` 固定 40px 宽 —— 名称现在由用户定,长名字会被压扁,改成 `max-width:112px` + 省略号。
- 缓存串升到 **`?v=20260924d`**;验收脚本 `check-habits.js`:**80 项全通过**。

**验收脚本踩过的坑(写下来免得下次再撞)**

- 用 jsdom 加载真实 DOM 时,`win.eval()` 灌完脚本后 `readyState` 仍是 `loading`,
  `init()` 挂在 `DOMContentLoaded` 上还没跑。**必须先 `await` 到 window 的 `load` 事件再断言**,
  否则所有按钮都停在 HTML 里的初始样子,会误判成一堆功能没实现(这次首轮 12 项「失败」全是这么来的,
  应用一行没改)。这和 09-22 那次「3 项假失败」是同一类问题:先怀疑脚本,再怀疑应用。
- 静态检查「有没有调用某个 API」之前要先剥掉注释,否则注释里写的「禁止 xxx()」会被当成真调用。
- 跑验收脚本要**显式给 `NODE_PATH`**(jsdom 装在 node 的 workspace 下,不在项目里):
  ```
  cd <项目根>
  NODE_PATH="<node 安装目录>/workspace/node_modules" node .cache/check-xxx.js
  ```
  本机的 Git Bash `PATH` 是坏的(`ls`/`tail` 都没有),所以实际执行时用的是 node 的**绝对路径**;
  换机器时把上面两个占位符换成自己机器上的位置即可。

**09-22 数据管理说明**

- 页脚新增三个按钮：导出 JSON / 导入 JSON / 清空全部数据，外加一个提示位与「数据面板」。
- **导出** `storage.buildExport()`：`{ app, schemaVersion:1, exportedAt, data:{六类}, meta }`，
  下载文件名 `个人工作台备份-YYYYMMDD-HHmm.json`；临时 Blob URL 用完立即 `revokeObjectURL`。
  **只有结构化数据，不含任何图片 / base64**（已验证导出文本里搜不到 `base64` / `data:image` / `blob:`）。
- **导入**：`validateImport()` → 校验 JSON 合法性、顶层是对象、`schemaVersion` 必须存在且**等于 1**
  （过新/过旧都拒绝并说明原因，现有数据不动）、`data` 段存在、六类形状正确（数组/数组/…/对象）、`meta` 是对象。
  通过后在**页脚数据面板**里展示摘要（导出时间、版本、六类条数与缺失标注），再点「覆盖导入」或「取消」。
  某类在文件里缺失时会明确标注"导入后这一项会被清空"，让用户先看到再决定。
- **覆盖写入 `applyImport()`**：写前备份全部键，中途任一写入失败就**整体回滚**到写入前的内容，
  返回「写入失败（记账），已回滚到你原来的数据。」
- **清空 `clearAll()`**：只对 `KEYS` 里列出的 7 个 `workbench.*` 键逐个 `removeItem`，
  **从不调用 `localStorage.clear()`**；测试验证了「其他站点的 key 不被误伤」。
  交互上是两次确认：先弹面板列出将删除的内容 → 点「确认清空」→ 再走一次系统确认。
- **清空后的行为**：写入 `meta = { schemaVersion:1, linksSeeded:true }`，
  这样刷新后**预置的 4 个常用网站不会自动恢复**（否则用户会以为没清干净）。
- **写入失败提示**：`storage.set()` 失败时广播 `workbench:storage-error`，
  app.js 在页脚提示位显示「数据没能保存：浏览器存储可能已满或被禁用。建议先导出备份…」。
- 打磨：删除确认文案统一为「…删除后无法恢复。」；明细金额列加宽到 112px 并右对齐 + 等宽数字；
  三张汇总卡的数字加 `min-width` 与右对齐。

## 6. 决策记录(Decisions)

| 决策 | 备选方案 | 选择理由 | 日期 |
|---|---|---|---|
| 单文件 HTML、零依赖 | 框架 + 构建 | 双击即用,无工具链维护成本 | 2026-09-08 |
| 金额以「分」存整数(8650 = ¥86.50) | 浮点 | 避免浮点误差;PRD §6 与 AGENTS 第 50 行已统一 | 2026-09-13 |
| 本周从周一开始 | 周日 | 与国内工作周一致 | 2026-09-11 |
| Tesseract 本地打包 + 懒加载 | CDN | 离线可用;代价是仓库 +20~40MB | 2026-09-11 |
| 文本片段 key 用 `workbench.snippets` | `workbench.text` | AGENTS 定名,PRD 沿用 | 2026-09-13 |
| **现在不下载 Tesseract 语言包** | 提前下好 | yang 明确要求保持仓库轻量,等做 OCR 那天再拉 | 2026-09-11 |
| Day 8 起改本地预览/静态服务器 | 继续 file:// | file:// 下剪贴板 API 不可用 | 2026-09-13 |
| 笔记删除后**保留**待办 noteId,显示「原笔记已删」 | 自动解绑(noteId 置空) | yang 09-17 指示:保留痕迹,不静默改数据;代价是与 PRD §2 原写法冲突,需改 PRD | 2026-09-17 |
| 常用网站**只预置一次**(`meta.linksSeeded`) | 每次为空就补预置 | 用户主动删掉的预置不该被自动恢复;代价是新增一条 meta 字段 | 2026-09-18 |
| 打卡**日/周分两张表**存进度 | 一张表用不同前缀的键 | 周一既是周起点又是当天,同表同键会串数据;代价是多一层结构 | 2026-09-20 |
| 打卡**学习=多次型** | PRD §5 原写"一次型" | yang 09-20 指示;学习要按次数累加才有意义 | 2026-09-20 |
| `storage.js` 的 `DEFAULTS.habits` 改为对象 | 保持数组 | 打卡要存"配置+记录",数组装不下;此前无写入故无需迁移 | 2026-09-20 |
| 记账金额存「分」的**整数（数字类型）** | 存「元」的小数 | PRD §6 与 AGENTS 都写"以分为单位的整数";整数也是数字,满足"不用字符串"。存元会有浮点误差 | 2026-09-20 |
| OCR 依赖**走 CDN**（改动 PRD 原决定） | 本地打包 `vendor/tesseract/` | yang 09-21 指示用 CDN + 锁版本;代价是离线/CDN 不通时 OCR 不可用,PRD 那句"不从 CDN 取用"需同步修改 | 2026-09-21 |
| OCR 版本锁定 **tesseract.js@7.0.0 + core@7.0.0 + traineddata 4.0.0** | 用 latest 别名 | 四者必须成套;`tesseract.js-core` 的 latest 标签停在 6.1.2,不能凭标签选版本,已逐条验证地址可达 | 2026-09-21 |
| 导入采用**严格校验 + 覆盖前回滚** | 直接写入 / 部分写入 | PRD 要求"失败时保留现有数据";写前备份全部键,任一失败整体回滚 | 2026-09-22 |
| 清空后写入 `meta.linksSeeded = true` | 保持彻底空白 | 否则刷新后预置网站会自己回来,用户会以为没清干净 | 2026-09-22 |
| 数据管理逻辑放进 **storage.js** | 放在 app.js 里直接读各 key | AGENTS 要求业务模块只经 storage.js;导出/清空本质是存储层职责 | 2026-09-22 |
| **番茄钟从"明确不做"放宽为"极简节拍器"** | 维持禁令 / 做完整番茄钟 | yang 09-23 要求;极简版纯前端零依赖、不碰数据结构(约 60 行),完整版会拖出统计/通知/续跑一堆范围 | 2026-09-23 |
| **番茄钟内嵌今日看板,不加左侧第 7 项** | 新增导航项 | yang 09-23:打开即见、少一次点击;同时保持 AGENTS「左侧 6 项导航」的既有约定不被打破 | 2026-09-23 |
| 待办优先级**新增 `priority` 字段但不动 schemaVersion** | 递增到 2 并写迁移 | 缺字段按「中」处理即可向后兼容,旧数据不用迁移;真要改结构时再整批升版本 | 2026-09-23 |
| 番茄钟代码**放在 `js/app.js`**,不新建 `js/pomo.js` | 单独建一个模块文件 | 它不读写 storage、不算业务模块;按 AGENTS「app.js 管启动与看板」的分工更顺,也避免动那张已稳定的目录树 | 2026-09-23 |
| 番茄钟到点**不自动切到下一段** | 专注结束自动进休息 | 自动切会顺带引出「要不要出声/要不要通知」一串范围;先只给文字提示,真想要再加 | 2026-09-23 |
| **视觉基调从「冷灰蓝」改为「奶油暖色系」** | 保留冷灰蓝 / 只借手感不改色相 | yang 09-24 给出参考图并明确选「照搬暖色调」。这是他自己定的约束,已当面说明冲突,由他决定作废 | 2026-09-24 |
| **颜色全部收进 CSS 变量,组件里禁止硬编码** | 继续局部硬编码 | 深色模式要求同一套变量在两处取值;任何写死的颜色在深色下都会露浅底 | 2026-09-24 |
| **深色模式纳入范围**(算新功能,不只是样式) | 只换浅色皮肤 | yang 09-24 明确选了「视觉 + 图标 + 深色模式」;实现上只是变量覆盖,不碰数据结构 | 2026-09-24 |
| 主题偏好存 `workbench.meta.theme` | 新增 `workbench.theme` key | AGENTS 的 key 清单是 7 个,不想扩;meta 本来就是"设置"容器 | 2026-09-24 |
| 图标用**内联 SVG** | 图标字体 / 图片文件 | 零依赖原则;能跟 `currentColor` 变色,深浅两套不用换图 | 2026-09-24 |
| 参照图里卡片顶部的「贴纸」小色块**不做** | 照做 | 纯装饰,与他"不要装饰性噪音"的既有偏好相冲;他要就说一声 | 2026-09-24 |
| **静态资源加 `?v=` 版本号** | 保持裸路径,靠 Ctrl+F5 | 服务端没有 `Cache-Control`,浏览器会启发式复用旧 CSS。实测已经害了一次(新 HTML + 旧 CSS → 满屏黑块)。换 URL 即换缓存键,一次投入长期受益 | 2026-09-24 |
| **内联 SVG 自带尺寸与描边属性** | 只靠 `.icon` 类 | CSS 过期/加载失败时,只靠类名的图标会退化成 300×150 的纯黑块;属性兜底后最差也只是"没风格" | 2026-09-24 |
| **打卡从「四类固定」改为用户可增删改** | 维持四类固定 | yang 09-24 明确要求;原 PRD §5 的"不让增删种类"作废并重写。代价是打卡要有空状态、要处理"删空后不能自动种回" | 2026-09-24 |
| **删除打卡项 = 连同它的历史记录一起删** | 只删配置、归档记录 | 与项目其它模块的删除语义一致(删就是删);否则会留下永远看不到的孤儿数据。代价是不可恢复,所以确认框里必须写明会删掉几个周期的记录 | 2026-09-24 |
| **老数据的四个 key 不迁移、不换 id** | 统一改成随机 id | 记录是按 id 存的,**换 id 就是丢历史**;老 config 缺的 label/kind 读取时补上即可,零迁移成本 | 2026-09-24 |
| 打卡名称用**可直接编辑的标题输入框** | 加「编辑」按钮弹窗 | 最直接、少一次点击;靠"悬停才显虚线边"表明它可编辑,不干扰阅读 | 2026-09-24 |
| 打卡加**「−1」纠错按钮**(一次型=撤销) | 长按撤销 / 不做 | yang 09-24 要求。不做二次确认(减错再 +1 即可,非不可逆);当前为 0 时禁用按钮,比"点完弹提示"清爽;减到 0 时删掉那条键,不留值为 0 的空记录 | 2026-09-24 |
| 站名用 **`1fr auto 1fr` 三栏网格**居中 | flex + `justify-content:center` | 左右两块宽度差很大(日期块 vs 番茄钟),flex 居中会被挤偏;三栏网格能保证中间那栏落在页面正中 | 2026-09-24 |
| 站名用**书法体**(方正舒体)+ 主色 + 薄荷记号笔 | 就用手边无衬线 / 加装饰图形 | yang 明确要"活波一点的艺术字、不要黑色、跟页面配色";书法体只有一个字重,所以 `font-weight` 必须写 400,否则合成加粗会糊 | 2026-09-24 |
| 昵称存 **`workbench.meta.nickname`** | 新增 `workbench.nickname` key | 与主题同一处,不扩 AGENTS 的 key 清单;且它算"显示偏好",清空数据时和主题一起保留 | 2026-09-24 |
| 待办改为**进度档位**(一条待办一个 `progress`) | 子任务推导(T20,已撤销)/ 只做顶部总进度条 / 拖滑块填百分比 | **T20 做错方向后改的**:yang 的参考图要的是"每条一个进度条 + 20/40/60/80/完成 五个按钮",并明确不要子待办、不要总进度条。我原先"手动维护百分比存活率低"的判断只对**拖滑块**成立,点档位不成立 —— **当场收回** | 2026-09-24 |
| `done` 变成 `progress` 的**派生值** | 两个字段各自独立维护 | 读取统一走 `isDone()`(只看 progress),写入时同步两者。两个真相来源迟早打架,会冒出"进度 100% 却显示未完成"这种自相矛盾状态 | 2026-09-24 |
| 点「完成」后**延迟 700ms 再重排** | 立刻重排 / 不做过渡 | 翻转后那条会跳到已完成区,立刻重排会让用户以为自己点错了。先把该卡片原地更新,再延时重排 + 淡入 | 2026-09-24 |
| ~~子任务 id 兜底用**下标**而不是随机值~~ | — | **随 T20 一并撤销**。但规律仍成立:任何"从脏数据兜底生成 id"的地方,兜底值必须**确定性**,否则每次渲染 id 都变、点上去找不到那一项 | 2026-09-24 |

## 7. 待澄清(Open Questions)

| # | 问题 | 我的倾向 | 状态 |
|---|---|---|---|
| 1 | 打卡四类是否要改成"时长"记录(当前:喝水多次型按杯,运动/学习/工作一次型) | 先用一周一次型,真想看"学了多久"再加(加法安全,减法丢数据) | 未定 |
| 2 | `file://` 下剪贴板不可用的兜底 | 默认手动复制;若常用,我写个 `.bat` 一键起本地服务器 | 未定 |
| 3 | 存储小实验拆骨架时删还是改 | **已解决:09-15 拆骨架时直接删除**,它不是业务模块,没必要为它保留 storage 通道 | 已定 |
| 4 | 待办的"删除"是否要二次确认 | 已按 PRD 用 `confirm()` 二次确认;若嫌打断,可改成 toast + 撤销 | 已定(09-16) |
| 5 | 看板汇总"无数据"该显示 `0` 还是 `—` | 接真实数据前改回 `—`(PRD 口径);待办卡片现在无数据时显示文案"还没有待办" | 未定 |
| 6 | PRD §2 的"笔记被删自动解绑 noteId"是否作废 | **已定:09-17 yang 拍板保留 noteId + 显示「原笔记已删」**,PRD §2 已同步改写 | 已定 |
| 7 | PRD §4 没有写"首次预置 4 个网站 + meta.linksSeeded" | 建议在 PRD §4 补一行,否则预置行为只存在于代码里 | 待确认 |
| 8 | 资料库里那份 09-14 的 `index` 快照怎么处置 | **已定(09-18):先不动**。资料库与本地项目无同步机制,那份只是历史留档;等 Day 18–19 部署前检查时再打包 zip 一次性更新 | 已定 |
| 9 | PRD §5 写着"运动/学习/工作=一次型",但 09-20 定的是学习=多次型 | **已定(09-24):PRD §5 整节重写**,类型分配改为"预置四项按 喝水/学习=多次型、运动/工作=一次型",且类型现在用户可改 | 已定 |
| 10 | 打卡**没有"减一/撤销"**,误点 +1 无法回退 | **已定(09-24):加了「−1」按钮**。多次型减到 0 为止不留 0 值记录;一次型达标时减 1 = 撤销完成;当前为 0 时按钮禁用。不做二次确认——减错再点 +1 就回来了 | 已定 |
| 11 | 打卡页与看板「打卡一览」**没有空状态** | **已定(09-24):打卡项可增删后必须补空状态**,两处都补了;看板汇总卡在总数 0 时显示「还没有打卡项」 | 已定 |
| 12 | 记账**只能删除、不能编辑**,但 PRD §8 写"记账允许编辑已有记录" | 本次需求只要求"可删除",故未做编辑。建议补一个编辑（或改 PRD §8） | 待确认 |
| 13 | 明细备注在列表里被单行截断,无处看全文 | 建议列表允许备注换行显示,或加一个选中查看的详情区。当前先截断 | 待确认 |
| 14 | PRD「技术栈」写着 OCR **本地打包、不从 CDN 取用**,与 09-21 改为 CDN 冲突 | **建议改 PRD**:去掉"不从 CDN 取用"与 Day 20 上传 vendor/ 的检查项,改为"锁定版本的 CDN,路径集中在 ocr.js 的 PATHS" | 待确认 |
| 15 | OCR 首次需下载约 36MB,慢网下体验差 | 保持现状(懒加载+浏览器缓存);若嫌慢,可选更小的 `4.0.0_fast` 语言模型或改本地打包 | 待确认 |
| 16 | 清空后预置网站不自动恢复,是否符合预期 | **已按"清空=真的空"实现**(写 linksSeeded=true);若想"恢复出厂"把预置带回来,改一行即可 | 待确认 |
| 17 | 番茄钟放看板的**哪个位置** | **已定(09-23):hero 右侧**,圆环小挂件;不动"下方两块同宽同高"的布局 | 已定 |
| 18 | 番茄钟**切走页面后是否继续计时** | 按"不续跑"实现(刷新即归零);但**标签页只是切到后台**时按真实时刻继续,不能靠每秒减一 | 已定 |
| 19 | 番茄钟要不要**自动切换**到下一段(专注结束自动进休息) | **暂时不自动**:到点只给文字提示,由用户点「休息 5」。自动切会引出"要不要出声提醒"的一串范围;真想要再加 | 未定 |
| 20 | PRD §5 打卡类型、§6 OCR 本地打包、§8 记账编辑三处口径与实现不一致 | **已解决(09-24)**:PRD 整体重写对齐实现(**8 处**)。其中 §8"记账/快捷入口可编辑"、空状态"无数据显示 `—`"两处是**当初写进 PRD、实现时却没做** —— 一律按**实现**改,而不是反过来去补功能 | 已定 |
| 21 | 部署源目录**要不要继续用项目根**(代价:`.workbuddy/memory/`、三份 md 文档连同目录列表一起公开可读) | 倾向**继续用根目录**:另建一个只含公开文件的发布目录,换来的是"改完根目录忘了同步"这个更高频的坑。真要干净,再改成发布目录 + 一条同步命令 | 待确认 |
| 22 | **要不要做多用户版**(登录 + 每人一份云端数据) | **已定(09-24):不做。** yang 认可「一个页面、各存各的浏览器」已经满足"别人也能用、数据互不干扰"。四条边界已写进 PRD 新章节(无法认人 / 同机同浏览器共用一份 / 换机与清缓存即丢且服务端无备份 / 无法替别人恢复)。**注意用词:没有"登录"这个动作** —— 隔离单位是**浏览器**不是人。要跨设备或认人才需要后端,那是另一个项目 | 已定 |
| 23 | 参照图的**左侧栏更宽**(约 250px)、卡片顶部贴纸等细节要不要继续跟进 | 栏宽已从 208 提到 224;贴纸不做。都属视觉微调,他看过实际效果再说 | 未定 |
| 24 | 深色模式**首次加载会有极短浅色闪烁**(脚本在底部加载,主题在 init 时才写进 `<html>`) | 目前接受。真要消除,需要在 `<head>` 里插一小段内联脚本先把属性写上——但那违反"脚本统一放底部"的约定,不值得 | 待确认 |
| 25 | 左栏**字体换成哪个方案** | **已定(09-24):方案 D** —— 不换字体,靠字重/字距/主色拿鲜活;理由是跨设备最一致,不会出现"我这儿好看、别人那儿回退" | 已定 |
| 26 | `--font-serif` 第一顺位写的**霞鹜文楷本机没装** | **已解决(09-24)**:装上 v1.522 Regular(24.4 MB),装到**用户级**字体目录 `%LOCALAPPDATA%\Microsoft\Windows\Fonts`(不需要管理员,不碰系统目录)。★ **读 TTF 的 name 表验证家族名 = `LXGW WenKai`**,与 CSS `--font-serif` 第一位正好对上 → **不用改任何代码**。⚠️ 只装在本机,访客仍回退楷体;要人人一致得引 webfont(数 MB 起),**不做** | 已定 |
| 27 | 站名字体最终定哪个 | **已定(09-24):方案 D 微软雅黑特粗 + 大字距**。理由:跨设备最一致(雅黑/PingFang 几乎人人有),不依赖稀有字体;活泼感靠字重与字距拿。字体栈:`雅黑 → PingFang → Noto Sans SC` | 已定 |
| 28 | 站名字体的三个变量要一起改(`--font-title` / `--title-weight` / `--title-spacing`) | 已写进 CSS 注释。**单字重字体(幼圆、书法体)必须配 weight 400**,写 700 会被浏览器合成加粗糊掉;换字体时字距也要跟着调 | 已定 |
| 29 | **待办进度显示怎么做(T20 → T21)** | **已定(09-24)**:先按"子任务"实现(T20)后被 yang 否掉 → 改为**进度档位**(每条一个 `progress`,列表改卡片 + 20/40/60/80/完成 五档,点当前档位退回 0%)。子任务相关代码已整段移除,**遗留字段不主动删**。已并入 PRD §2 | 已定 |

---

_项目创建:2026-09-14 ｜ 最后更新:2026-09-24_
