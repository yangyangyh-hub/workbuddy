# **个人工作台 PRD**

## **产品概述**

「个人工作台」是一个单页网页。打开即见今日看板；左侧按
**今日看板 → 打卡 → 待办 → 快速笔记 → 记账 → 快捷入口** 切换。
今日看板的大日期右侧内嵌一个番茄钟（不占左侧导航）。

数据保存在当前浏览器、当前 origin 的 localStorage，**没有账号、没有后端**。

**部署状态（2026-09-24 更新）**：已提前部署为静态站点
`https://personal-workbench-89221.app.workbuddy.host/`（HTTPS）。
原计划「前 17 天本机使用、Day 20 再部署」已作废。

- **origin 变了，老数据不会自动跟过去**：本机调试用的 `127.0.0.1:8777` 与线上是两套 localStorage。
  迁移办法：旧地址「导出 JSON」→ 新地址「导入 JSON」。
- **改了 css/js 后如何让对方拿到新版**：引用带 `?v=` 缓存串，改版时同步改版本号即可，
  访问者不必强刷。**例外**：改 `index.html` 本身仍需一次 Ctrl+F5（入口文件没法给自己加版本号）。
- **公开目录**：发布源是整个项目根目录，即项目文档（PRD / AGENTS / PROJECT）与
  `.workbuddy/memory/` 都在公开可读范围内，且目录列表默认开启。里面没有密钥、证件或图片。

### **多人与共用链接的数据边界（2026-09-24 讨论后确定）**

链接是公开的，任何拿到链接的人都能打开。但数据隔离的维度是**浏览器，不是人** ——
每个访问者看到的是自己浏览器里的空白工作台，数据互不相通。

**这不是"多用户系统"**，准确说法是「**一个页面，各存各的浏览器**」。
本项目明确**不做**账号 / 登录 / 后端（不引入邮箱登录、不做微信小程序）。
因此有四个必须接受的边界：

1. **无法认人**：不知道谁在用、无法限制谁能用、无法停用某人
2. **同一台电脑 + 同一个浏览器 = 同一份数据**：两人共用一台机器同一浏览器，会看到彼此的内容
3. **换电脑、换浏览器、清浏览器缓存 = 数据不跟过去**；清了「网站数据」就彻底没有，
   **服务端没有备份可恢复**
4. **无法替别人恢复数据**，也无从知道别人存了什么

如果以后需要「跨设备接着用」或「知道谁在用」，才需要引入后端与账号 ——
那是另一个项目，不在本 PRD 范围。

## **核心功能（MVP）**

### **1. 今日看板**

- 打开首页默认显示；左侧第一项也是今日看板
- 站名（「个人工作台」）居中偏左紧贴大日期，用雅黑特粗 + 大字距；左栏顶部是「Hi，昵称」（昵称自己填）
- 当天日期、星期、时段问候
- 大日期右侧内嵌**番茄钟**（不占左侧导航，不放第三个面板，见下方专门小节）
- 汇总：待办进度、今日收入与支出及本周/本月、打卡当前周期达标情况
- 下方两块同宽同高：本日待办、打卡一览

**番茄钟（极简节拍器，2026-09-23 新增）**

- 位置：今日看板 hero 右端。**不新增左侧导航第 7 项**
- 形状：圆环 + `MM:SS` 数字 + 专注 25 / 休息 5 两个切换钮 + 开始 / 暂停 / 重置三个按钮
- 时长写死 25 / 5，**不可自定义**
- **纯内存**：不写 localStorage、不进导出 JSON、刷新或关掉标签页即归零
- 计时口径：按「目标结束时刻」推算，不是每秒减一。标签页切后台被节流时不会走慢，回到前台一步校准
- 明确不做：番茄计数与统计、铃声与系统通知、自动切换到下一段、与打卡联动、跨刷新继续计时
- 时间到只把圆环走完、数字归零并给一句文字提示，不弹窗、不出声

### **2. 待办**

- 字段：id, bucket（today|week|month）, text, done, priority（high|mid|low）, progress, deadline（可空）, noteId（可空）, createdAt
- 三个 Tab，用户自己放，不自动分
- **进度档位（2026-09-24 新增）**：`progress` 取值 **0 / 20 / 40 / 60 / 80 / 100**，100 即「完成」。
  一条待办只有**一个进度值**，不是「完成 / 未完成」两态
  - 界面上是四个档位按钮 + 一个「完成」按钮
  - **再点一次当前档位 = 退回 0%**：界面上没有「0%」按钮，靠这个反转回到未开始；
    「完成」按钮走同一条逻辑，所以完成后再点它就是取消完成
  - `done` 是 `progress` 的**派生值**：写入时同步维护，读取时一律用 `isDone()` 判断
    （只看进度，不看 `done` 字段），因此不会出现「进度 100% 却显示未完成」这种矛盾
  - **旧记录没有 `progress`** → 按 `done` 反推（`true`→100 / `false`→0），
    **不动 schemaVersion、不写迁移**（同 `priority` 那次）
  - 脏值兜底：`progress` 不是数字 → 回落 `done`；是数字但不在档位上 → 归到**最近的档位**
    （99 → 100、85 → 80）。**正中间取较低档位**（30→20、50→40、90→80）——
    宁可少算完成，也不把 90% 凭空判成「已完成」；完成必须是用户点出来的明确动作
  - 列表改为**卡片**：① 优先级 + 标题 + 截止日期 + 删除 ② 进度条 + 百分比 ③ 档位按钮
  - 卡片与详情区**共用同一套档位按钮**，两处实时同步；看板「本日待办」每条跟一条迷你进度条
- **历史：子任务已撤销**。2026-09-24 曾短暂实现过 `subtasks: [{ id, text, done }]`
  （有子任务时整条完成状态由子任务推导），当天按 yang 的要求**撤销**，
  改为「一条待办 = 一个进度值」。存储里遗留的 `subtasks` 字段**一律忽略、不主动删除** ——
  删用户数据是不可逆动作，不做
- **翻转后的排队过渡**：点「完成」使整条变成已完成、按排序要跳到已完成区时，
  **不是立刻重排**——先把该卡片原地更新（进度条、档位高亮、完成样式立即可见），
  700ms 后再重排并给移动的卡片一个淡入。避免"一点它就飞走、以为自己点错了"
  （遵循 `prefers-reduced-motion`）
- **优先级（2026-09-23 新增）**：取值 `high`（高）/ `mid`（中）/ `low`（低），新增时默认「中」。
  旧记录没有这个字段，读取时按「中」处理，**不动 schemaVersion、不做数据迁移**
  - 卡片左侧一个可点的小标，点一下循环切换：高 → 中 → 低 → 高
  - 详情区下拉可选；改完点「保存」生效
  - 排序口径：未完成在前，其次按 高 → 中 → 低，最后按创建时间先后
  - 看板「本日待办」同一栏显示同一枚小标（只读，不可点）
- 截止日期选填；未填显示「未填截止日期」；已填显示还剩 N 天
- 日期按用户本地时间计算；本周从周一开始。今天到期显示「今天到期」，过去日期显示「已过期 N 天」
- 可关联一条笔记并跳转；提交时间到秒，不可改
- 空字符串不要添加
- **切 Tab（本日 / 本周 / 本月）只是筛选，不自动移动任务**，任务留在用户自己选的那个 Tab 里。
  逾期提示**只依据「截止日期」**（显示「已过期 N 天」），与所在 Tab 无关；
  当前实现**没有**基于 Tab 归属的逾期标记
- 已关联的笔记被删除时，**保留该待办原有的 noteId，不清空**；在关联处显示「原笔记已删」并隐藏「查看笔记」跳转入口，不做静默解绑

### **3. 快速笔记**

- 字段：id, cause（可空）, body, createdAt
- 写完点提交才保存；自动记录时间
- 提交后**不可编辑**，写错只能删除重录

### **4. 快捷入口**

- 常用网站：id, name, url；无协议补 https://
- 常用文本：id, name, text；一键复制
- 网站只允许 http:// 或 https://，其他协议拒绝保存
- **不支持编辑已有条目**：目前只有「添加」与「删除」（见 §8）

### **5. 打卡**

- **打卡项由用户自己增删改**（2026-09-24 改版；原写法是「运动/喝水/学习/工作四类固定，不让增删种类」）
- 每项字段：`label` 名称（≤12 字）、`kind` 类型（`count` 多次型 / `once` 一次型）、`period` 周期（`day` 每日 / `week` 每周）、`target` 目标次数
- 首次使用的预置四项（与老版本一致）：**喝水 = 多次型 / 每日 / 目标 8**；**运动 = 一次型 / 每日**；**学习 = 多次型 / 每日 / 目标 1**；**工作 = 一次型 / 每日**
- **把这四项全删光之后不会自动恢复**：需要一条 `initialized` 标记区分「从没用过」与「被删空」。只按"配置为空就补默认值"判断的话，用户会以为删不掉
- 名称在卡片标题上直接改，失焦即保存；**名称不允许改成空**，会被拒绝并回退原值
- 类型可随时改；**切成一次型时目标强制回 1**，切回多次型时目标至少为 1（不允许出现 0）
- 一次型的目标固定为 1，界面上不给改（输入框直接禁用）；多次型才允许设置目标次数
- **可回退（2026-09-24 新增）**：每张卡片有「−1」按钮，用来纠正误点
  - 多次型：次数 −1，**减到 0 为止**，不会出现负数；减到 0 时把存储里那条键删掉，不留值为 0 的空记录
  - 一次型达标时值为 1，减 1 就是**撤销完成**（按钮上有说明性 tooltip）
  - 当前为 0 时按钮**直接禁用**（比"点完再弹提示"清爽）
  - −1 不需要二次确认：减错了再点一次 +1 就回来了，不构成不可逆操作
- **删除打卡项 = 连同它的历史记录一起删除**，删除前二次确认，并在确认文案里写明「会连同 N 个周期的记录一起删除」
- 每日目标按本地自然日重置；每周目标每周一 00:00 开始新周期；**换周期不删旧周期数据**，历史一直保留
- 进度在**渲染时按当前时间实时计算**；页面长时间开着跨过 0 点或周一 00:00 时，无需刷新即进入新周期
- 一个打卡项都没有时，打卡页与看板「打卡一览」都显示空状态（此前四类固定，不可能为空，所以没有空状态）

### **6. 记账 + OCR**

- 字段：id, amount, type（in|out）, category, note, createdAt, source
- 展示今日/本周/本月的收和支
- OCR 必须确认卡才入账；不存原图
- 本周按周一到周日计算；分类必填，备注选填
- 金额以**「分」为单位的整数**存储，渲染时除以 100；不使用浮点数存元
- 分类使用固定枚举，用户不可自定义：支出 = 餐饮 / 交通 / 购物 / 居住 / 医疗 / 其他；收入 = 工资 / 报销 / 其他
- **不支持编辑已有记录**：目前只有「记一笔」与「删除」（见 §8）
- OCR 确认流：
  - 识别结果先进**独立暂存区**，确认前**不计入**今日 / 本周 / 本月统计
  - 确认卡上的金额、类型、分类**可编辑**
  - 识别失败或金额解析不出时，允许**手动输入**直接入账
  - 暂存项落 localStorage（仅文本结果，不存图片），刷新后仍在
  - 同一张截图重复粘贴时给出重复提示，由用户决定是否仍然入账
- OCR 依赖（Tesseract 主库 + worker + core + chi_sim/eng 语言包）**从 CDN 取用并锁定版本**，
  首次点击识别时才加载（懒加载）并显示进度；未点击识别时完全不加载，不影响页面打开速度。
  锁定的具体版本与地址见 `README.md` 的对照表 —— **四者必须成套，不要改成 `latest` 或浮动别名**

### **7. 数据管理**

- 导出 JSON：包含 schemaVersion、exportedAt、全部业务数据和必要设置
- 导入 JSON：先校验和展示摘要，确认后覆盖；失败时保留现有数据
- 清空全部数据：二次确认，只清理本项目的 workbench.* key。
  **主题与昵称属于「显示偏好」，清空数据时保留**（否则界面会突然变回浅色，看起来像出了 bug）
- 当前 schemaVersion = 1；后续结构变更必须递增版本号并保留旧数据迁移逻辑，不允许直接丢弃读不懂的数据

### **8. 编辑与删除**

- 待办、笔记、记账、快捷入口均支持**单项删除**，删除前二次确认
- **可编辑的只有待办**（详情区改内容 / 优先级 / 截止日期 / 关联笔记，另有进度档位与优先级小标）
- **笔记提交后不可编辑**，只能删除重录
- **记账、快捷入口（网站与常用文本）不支持编辑**，只能删除后重新添加
  （2026-09-24 按实现修正；此前 PRD 写的是"待办、记账、快捷入口允许编辑"，与实现不符 ——
   若要给这两处补编辑功能，属于新需求，不在当前范围）

## **界面**

- **书桌垫板**：奶油纸底 `#faf6ea` + 细点阵纹理
- 卡片 `#fffdf7`、圆角 16px、几乎无边框与阴影，靠色块区分模块：
  薄荷 `#d8f0e4` / 浅粉 `#fbdfe2` / 杏黄 `#fcf0ce`
- 强调色**深墨绿** `#2f7a5e`（标题、数字、进度条、选中态）
- 左侧导航白底 + 选中项做成薄荷色 pill，每项前带一个内联 SVG 线性图标（零依赖）
- **深色模式**：左下角开关，偏好存 `workbench.meta.theme`；深色与浅色**共用同一套 CSS 变量**，
  改配色只改 `css/style.css` 顶部那两个变量块。**新增颜色必须走变量**，写死的颜色在深色下会露浅底
- **字体**：站名与产品名用雅黑特粗 + 大字距；大日期用 `--font-serif`（首选霞鹜文楷 `LXGW WenKai`）；
  金额与数字用等宽 `--font-num`
- 金额等宽数字。待办列表与详情左右分栏（列表栏略宽，`1.35fr : 1fr`，为了放得下档位按钮）
- 不要口号、不要六宫格、不要手机页；**禁止紫粉渐变与玻璃拟态**
- ⚠️ **本机字体 ≠ 别人能看到**：霞鹜文楷装在**你自己的电脑**上，别人的电脑没有，会回退到字体栈里的下一个
  （备选依次为楷体 / KaiTi）。想让人人一致必须引 webfont，那需要额外下载中文字体（数 MB 起），
  目前**不做**

## **空状态**

- 待办、笔记、记账、打卡、快捷入口五个模块在无数据时，各显示一句提示文案（如「还没有笔记，写一条试试」），不显示空白容器
- 今日看板的汇总卡在无数据时**数字显示 `0`**，卡片内的小字给出说明（如「还没有待办」「还没有打卡项」）。
  此前 PRD 写的"显示 `—` 不显示 `0`"**未实现**，2026-09-24 按实现修正 ——
  因为卡片小字已经承担了"没有数据"的表达，数字位保持 `0` 更符合看板直觉

## **失败态**

- localStorage 写入失败（隐私模式、配额写满）时：捕获异常并提示
  「数据没能保存：浏览器存储可能已满或被禁用。建议先导出备份，再清理空间重试。」，
  同时保留界面上已输入的内容，不静默丢弃
- 一键复制失败时（如 `file://` 下剪贴板接口不可用）：提示复制失败并展示文本供手动复制
- OCR 失败时：允许手动输入入账，流程不中断
- **语言包 / CDN 加载失败或超时时**：提示原因，并引导改为手动输入入账，不阻断记账功能。
  ⚠️ 已实测：**本机直连 `cdn.jsdelivr.net` 不通**（超时），开发者自己调试 OCR 时需要走代理；
  线上访客能否加载取决于其网络环境
- 导入失败时：保留现有数据，不做部分写入

## **技术栈**

HTML + CSS + JS，localStorage，Tesseract.js（chi_sim+eng）。不要 React/Vue/后端。
零框架、零构建，双击 `index.html` 即可运行。

OCR 依赖采用 **CDN + 版本锁定 + 懒加载**（2026-09-24 由"本地打包"改为现在的方案）：
原先计划把 Tesseract 主库、worker、core 与 chi_sim / eng 语言包放进项目内 `vendor/tesseract/`
随站点上传；实际改为从 CDN 取用并**逐个锁定版本**，好处是发布目录不必带 20–40 MB 二进制、
也不占仓库空间。代价是**首次识别必须联网**，且 CDN 的可达性受网络环境影响（见上）。

## **安全与数据边界**

- 用户输入按普通文本显示，不把输入内容直接拼入 innerHTML
- localStorage 是本机浏览器中的明文存储，不保存密码、API Key、身份证照片等高敏感信息
- 项目只保存结构化记录，不保存 OCR 原图
- **导出/导入是唯一的迁移与备份手段**：服务端不存任何业务数据，清理浏览器数据后无法找回
